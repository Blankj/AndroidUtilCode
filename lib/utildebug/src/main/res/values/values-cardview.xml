<?xml version="1.0" encoding="utf-8"?>
<resources>
    <attr format="reference" name="scardViewStyle"/>
    <color name="scardview_dark_background">#FF424242</color>
    <color name="scardview_light_background">#FFFFFFFF</color>
    <color name="scardview_shadow_end_color">#03000000</color>
    <color name="scardview_shadow_start_color">#37000000</color>
    <declare-styleable name="SCardView">
        <!-- Background color for CardView. -->
        <attr format="color" name="scardBackgroundColor"/>
        <!-- Corner radius for CardView. -->
        <attr format="dimension" name="scardCornerRadius"/>
        <!-- Elevation for CardView. -->
        <attr format="dimension" name="scardElevation"/>
        <!-- Maximum Elevation for CardView. -->
        <attr format="dimension" name="scardMaxElevation"/>
        <!-- Add padding in API v21+ as well to have the same measurements with previous versions. -->
        <attr format="boolean" name="scardUseCompatPadding"/>
        <!-- Add padding to CardView on v20 and before to prevent intersections between the Card content and rounded corners. -->
        <attr format="boolean" name="scardPreventCornerOverlap"/>
        <attr format="color" name="scardShadowColorStart"/>
        <attr format="color" name="scardShadowColorEnd"/>
        <!-- Inner padding between the edges of the Card and children of the CardView. -->
        <attr format="dimension" name="contentPadding"/>
        <!-- Inner padding between the left edge of the Card and children of the CardView. -->
        <attr format="dimension" name="contentPaddingLeft"/>
        <!-- Inner padding between the right edge of the Card and children of the CardView. -->
        <attr format="dimension" name="contentPaddingRight"/>
        <!-- Inner padding between the top edge of the Card and children of the CardView. -->
        <attr format="dimension" name="contentPaddingTop"/>
        <!-- Inner padding between the bottom edge of the Card and children of the CardView. -->
        <attr format="dimension" name="contentPaddingBottom"/>
        <!-- Workaround to read user defined minimum width -->
        <attr name="android:minWidth"/>
        <!-- Workaround to read user defined minimum height -->
        <attr name="android:minHeight"/>
    </declare-styleable>
    <dimen name="scardview_compat_inset_shadow">0dp</dimen>
    <style name="Base.SCardView" parent="android:Widget">
        <item name="scardCornerRadius">2dp</item>
        <item name="scardElevation">2dp</item>
        <item name="scardMaxElevation">2dp</item>
        <item name="scardUseCompatPadding">false</item>
        <item name="scardPreventCornerOverlap">true</item>
    </style>
    <style name="SCardView" parent="Base.SCardView">
    </style>
    <style name="SCardView.Dark">
        <item name="scardBackgroundColor">#FF424242</item>
    </style>
    <style name="SCardView.Light">
        <item name="scardBackgroundColor">#FFFFFFFF</item>
    </style>
</resources>